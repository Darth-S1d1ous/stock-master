# 由 scripts/gen_rules_from_std_to_base.rs 从 std_to_base.jsonl 生成（勿手改）。
# 源记录 1 条；base 工厂：GetFileLength。
id: CPP_BASE_REUSE_STD_FILESYSTEM_FILE_SIZE
enabled: true
name: "std::filesystem::file_size 的 base 库等价实现要求"
severity: warn
engine_requires: [ast]
applies_to:
  languages: [cpp]
  exclude_glob:
    - "**/third_party/**"
    - "**/vendor/**"
    - "**/tests/**"
patterns:
  - id: std_call
    description: "调用 std::filesystem::file_size（限定或裸调用；方法调用 .file_size() 不在内；filesystem::file_size 限定名消歧同名 API）"
    spec:
      type: Call
      value:
        name: "filesystem::file_size"
meta:
  rationale: "util::file::GetFileLength 通过路径获取文件大小（字节），与 std::filesystem::file_size 语义一致"
  fix_suggestion: "改用 base 库等价实现：GetFileLength"
  references:
    - "common/include/file_util.h"
  tapd_ids: ["1070112211134320991"]
  tags: ["base-reuse", "std-to-base", "generated"]
  std_signature: "std::filesystem::file_size"
  base_preferred: "util::file::GetFileLength"
  reason: "util::file::GetFileLength 通过路径获取文件大小（字节），与 std::filesystem::file_size 语义一致"
