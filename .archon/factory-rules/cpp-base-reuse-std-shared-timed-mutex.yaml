# 由 scripts/gen_rules_from_std_to_base.rs 从 std_to_base.jsonl 生成（勿手改）。
# 源记录 1 条；base 工厂：shared_timed_mutex。
id: CPP_BASE_REUSE_STD_SHARED_TIMED_MUTEX
enabled: true
name: "std::shared_timed_mutex 的 base 库等价实现要求"
severity: warn
engine_requires: [ast]
applies_to:
  languages: [cpp]
  exclude_glob:
    - "**/third_party/**"
    - "**/vendor/**"
    - "**/tests/**"
patterns:
  - id: std_call
    description: "调用 std::shared_timed_mutex（限定或裸调用；方法调用 .shared_timed_mutex() 不在内；shared_timed_mutex 限定名消歧同名 API）"
    spec:
      type: Call
      value:
        name: "shared_timed_mutex"
meta:
  rationale: "base::shared_timed_mutex 提供共享/独占两级访问的带超时互斥锁，与 std::shared_timed_mutex 的读写锁语义完全对应"
  fix_suggestion: "改用 base 库等价实现：shared_timed_mutex"
  references:
    - "common/include/shared_mutex.h"
  tapd_ids: ["1070112211134320991"]
  tags: ["base-reuse", "std-to-base", "generated"]
  std_signature: "std::shared_timed_mutex"
  base_preferred: "base::shared_timed_mutex"
  reason: "base::shared_timed_mutex 提供共享/独占两级访问的带超时互斥锁，与 std::shared_timed_mutex 的读写锁语义完全对应"
