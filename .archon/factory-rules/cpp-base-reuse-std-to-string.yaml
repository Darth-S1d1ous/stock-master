# 由 scripts/gen_rules_from_std_to_base.rs 从 std_to_base.jsonl 生成（勿手改）。
# 源记录 1 条；base 工厂：NumToString。
id: CPP_BASE_REUSE_STD_TO_STRING
enabled: true
name: "std::to_string 的 base 库等价实现要求"
severity: warn
engine_requires: [ast]
applies_to:
  languages: [cpp]
  exclude_glob:
    - "**/third_party/**"
    - "**/vendor/**"
    - "**/tests/**"
patterns:
  - id: std_call
    description: "调用 std::to_string（限定或裸调用；方法调用 .to_string() 不在内；to_string 限定名消歧同名 API）"
    spec:
      type: Call
      value:
        name: "to_string"
meta:
  rationale: "std::to_string 数字转字符串；base util::convert::NumToString 提供等价转换"
  fix_suggestion: "改用 base 库等价实现：NumToString"
  references:
    - "common/include/convert_util.h"
  tapd_ids: ["1070112211134320991"]
  tags: ["base-reuse", "std-to-base", "generated"]
  std_signature: "std::to_string"
  base_preferred: "util::convert::NumToString"
  reason: "std::to_string 数字转字符串；base util::convert::NumToString 提供等价转换"
