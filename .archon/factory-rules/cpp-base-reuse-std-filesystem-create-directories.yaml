# 由 scripts/gen_rules_from_std_to_base.rs 从 std_to_base.jsonl 生成（勿手改）。
# 源记录 1 条；base 工厂：MakeDir。
id: CPP_BASE_REUSE_STD_FILESYSTEM_CREATE_DIRECTORIES
enabled: true
name: "std::filesystem::create_directories 的 base 库等价实现要求"
severity: warn
engine_requires: [ast]
applies_to:
  languages: [cpp]
  exclude_glob:
    - "**/third_party/**"
    - "**/vendor/**"
    - "**/tests/**"
patterns:
  - id: std_call
    description: "调用 std::filesystem::create_directories（限定或裸调用；方法调用 .create_directories() 不在内；filesystem::create_directories 限定名消歧同名 API）"
    spec:
      type: Call
      value:
        name: "filesystem::create_directories"
meta:
  rationale: "std::filesystem::create_directories 递归创建目录；base util::file::MakeDir 提供等价能力"
  fix_suggestion: "改用 base 库等价实现：MakeDir"
  references:
    - "common/include/file_util.h"
  tapd_ids: ["1070112211134320991"]
  tags: ["base-reuse", "std-to-base", "generated"]
  std_signature: "std::filesystem::create_directories"
  base_preferred: "util::file::MakeDir"
  reason: "std::filesystem::create_directories 递归创建目录；base util::file::MakeDir 提供等价能力"
