# 由 scripts/gen_rules_from_std_to_base.rs 从 std_to_base.jsonl 生成（勿手改）。
# 源记录 1 条；base 工厂：GetRelativePath。
id: CPP_BASE_REUSE_STD_FILESYSTEM_RELATIVE
enabled: true
name: "std::filesystem::relative 的 base 库等价实现要求"
severity: warn
engine_requires: [ast]
applies_to:
  languages: [cpp]
  exclude_glob:
    - "**/third_party/**"
    - "**/vendor/**"
    - "**/tests/**"
patterns:
  - id: std_call
    description: "调用 std::filesystem::relative（限定或裸调用；方法调用 .relative() 不在内；filesystem::relative 限定名消歧同名 API）"
    spec:
      type: Call
      value:
        name: "filesystem::relative"
meta:
  rationale: "util::file::GetRelativePath 获取相对路径，与 std::filesystem::relative 语义一致"
  fix_suggestion: "改用 base 库等价实现：GetRelativePath"
  references:
    - "common/include/file_util.h"
  tapd_ids: ["1070112211134320991"]
  tags: ["base-reuse", "std-to-base", "generated"]
  std_signature: "std::filesystem::relative"
  base_preferred: "util::file::GetRelativePath"
  reason: "util::file::GetRelativePath 获取相对路径，与 std::filesystem::relative 语义一致"
