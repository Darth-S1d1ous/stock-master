# 由 scripts/gen_rules_from_std_to_base.rs 从 std_to_base.jsonl 生成（勿手改）。
# 源记录 1 条；base 工厂：HashString。
id: CPP_BASE_REUSE_STD_HASH
enabled: true
name: "std::hash 的 base 库等价实现要求"
severity: warn
engine_requires: [ast]
applies_to:
  languages: [cpp]
  exclude_glob:
    - "**/third_party/**"
    - "**/vendor/**"
    - "**/tests/**"
patterns:
  - id: std_call
    description: "调用 std::hash（限定或裸调用；方法调用 .hash() 不在内；hash 限定名消歧同名 API）"
    spec:
      type: Call
      value:
        name: "hash"
meta:
  rationale: "util::crypto::HashString 计算数据的通用哈希值返回 uint64_t，与 std::hash 的通用哈希函数语义对应"
  fix_suggestion: "改用 base 库等价实现：HashString"
  references:
    - "common/include/crypto_util.h"
  tapd_ids: ["1070112211134320991"]
  tags: ["base-reuse", "std-to-base", "generated"]
  std_signature: "std::hash"
  base_preferred: "util::crypto::HashString"
  reason: "util::crypto::HashString 计算数据的通用哈希值返回 uint64_t，与 std::hash 的通用哈希函数语义对应"
