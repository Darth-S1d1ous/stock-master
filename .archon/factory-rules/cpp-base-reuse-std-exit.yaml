# 由 scripts/gen_rules_from_std_to_base.rs 从 std_to_base.jsonl 生成（勿手改）。
# 源记录 1 条；base 工厂：Terminate。
id: CPP_BASE_REUSE_STD_EXIT
enabled: true
name: "std::exit 的 base 库等价实现要求"
severity: warn
engine_requires: [ast]
applies_to:
  languages: [cpp]
  exclude_glob:
    - "**/third_party/**"
    - "**/vendor/**"
    - "**/tests/**"
patterns:
  - id: std_call
    description: "调用 std::exit（限定或裸调用；方法调用 .exit() 不在内；exit 限定名消歧同名 API）"
    spec:
      type: Call
      value:
        name: "exit"
meta:
  rationale: "util::system::Terminate 接受 error_code 参数终止程序，语义对应 std::exit 的程序终止机制"
  fix_suggestion: "改用 base 库等价实现：Terminate"
  references:
    - "common/include/system_util.h"
  tapd_ids: ["1070112211134320991"]
  tags: ["base-reuse", "std-to-base", "generated"]
  std_signature: "std::exit"
  base_preferred: "util::system::Terminate"
  reason: "util::system::Terminate 接受 error_code 参数终止程序，语义对应 std::exit 的程序终止机制"
