# 由 scripts/gen_rules_from_std_to_base.rs 从 std_to_base.jsonl 生成（勿手改）。
# 源记录 1 条；base 工厂：RenameFile。
id: CPP_BASE_REUSE_STD_FILESYSTEM_RENAME
enabled: true
name: "std::filesystem::rename 的 base 库等价实现要求"
severity: warn
engine_requires: [ast]
applies_to:
  languages: [cpp]
  exclude_glob:
    - "**/third_party/**"
    - "**/vendor/**"
    - "**/tests/**"
patterns:
  - id: std_call
    description: "调用 std::filesystem::rename（限定或裸调用；方法调用 .rename() 不在内；filesystem::rename 限定名消歧同名 API）"
    spec:
      type: Call
      value:
        name: "filesystem::rename"
meta:
  rationale: "util::file::RenameFile 重命名文件，与 std::filesystem::rename 语义完全对应"
  fix_suggestion: "改用 base 库等价实现：RenameFile"
  references:
    - "common/include/file_util.h"
  tapd_ids: ["1070112211134320991"]
  tags: ["base-reuse", "std-to-base", "generated"]
  std_signature: "std::filesystem::rename"
  base_preferred: "util::file::RenameFile"
  reason: "util::file::RenameFile 重命名文件，与 std::filesystem::rename 语义完全对应"
