# 由 scripts/gen_rules_from_std_to_base.rs 从 std_to_base.jsonl 生成（勿手改）。
# 源记录 1 条；base 工厂：TruncateFile。
id: CPP_BASE_REUSE_STD_FILESYSTEM_RESIZE_FILE
enabled: true
name: "std::filesystem::resize_file 的 base 库等价实现要求"
severity: warn
engine_requires: [ast]
applies_to:
  languages: [cpp]
  exclude_glob:
    - "**/third_party/**"
    - "**/vendor/**"
    - "**/tests/**"
patterns:
  - id: std_call
    description: "调用 std::filesystem::resize_file（限定或裸调用；方法调用 .resize_file() 不在内；filesystem::resize_file 限定名消歧同名 API）"
    spec:
      type: Call
      value:
        name: "filesystem::resize_file"
meta:
  rationale: "util::file::TruncateFile 截断或调整文件到指定大小，与 std::filesystem::resize_file 语义对应"
  fix_suggestion: "改用 base 库等价实现：TruncateFile"
  references:
    - "common/include/file_util.h"
  tapd_ids: ["1070112211134320991"]
  tags: ["base-reuse", "std-to-base", "generated"]
  std_signature: "std::filesystem::resize_file"
  base_preferred: "util::file::TruncateFile"
  reason: "util::file::TruncateFile 截断或调整文件到指定大小，与 std::filesystem::resize_file 语义对应"
