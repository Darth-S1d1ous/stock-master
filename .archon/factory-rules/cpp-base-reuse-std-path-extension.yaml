# 由 scripts/gen_rules_from_std_to_base.rs 从 std_to_base.jsonl 生成（勿手改）。
# 源记录 1 条；base 工厂：GetFileExtName。
id: CPP_BASE_REUSE_STD_PATH_EXTENSION
enabled: true
name: "std::filesystem::path::extension 的 base 库等价实现要求"
severity: warn
engine_requires: [ast]
applies_to:
  languages: [cpp]
  exclude_glob:
    - "**/third_party/**"
    - "**/vendor/**"
    - "**/tests/**"
patterns:
  - id: std_call
    description: "调用 std::filesystem::path::extension（限定或裸调用；方法调用 .extension() 不在内；path::extension 限定名消歧同名 API）"
    spec:
      type: Call
      value:
        name: "path::extension"
meta:
  rationale: "util::file::GetFileExtName 从文件路径中提取扩展名，与 std::filesystem::path::extension 语义对应"
  fix_suggestion: "改用 base 库等价实现：GetFileExtName"
  references:
    - "common/include/file_util.h"
  tapd_ids: ["1070112211134320991"]
  tags: ["base-reuse", "std-to-base", "generated"]
  std_signature: "std::filesystem::path::extension"
  base_preferred: "util::file::GetFileExtName"
  reason: "util::file::GetFileExtName 从文件路径中提取扩展名，与 std::filesystem::path::extension 语义对应"
