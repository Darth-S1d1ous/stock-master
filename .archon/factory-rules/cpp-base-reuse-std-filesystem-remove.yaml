# 由 scripts/gen_rules_from_std_to_base.rs 从 std_to_base.jsonl 生成（勿手改）。
# 源记录 1 条；base 工厂：RemoveFile。
id: CPP_BASE_REUSE_STD_FILESYSTEM_REMOVE
enabled: true
name: "std::filesystem::remove 的 base 库等价实现要求"
severity: warn
engine_requires: [ast]
applies_to:
  languages: [cpp]
  exclude_glob:
    - "**/third_party/**"
    - "**/vendor/**"
    - "**/tests/**"
patterns:
  - id: std_call
    description: "调用 std::filesystem::remove（限定或裸调用；方法调用 .remove() 不在内；filesystem::remove 限定名消歧同名 API）"
    spec:
      type: Call
      value:
        name: "filesystem::remove"
meta:
  rationale: "util::file::RemoveFile 删除指定文件，与 std::filesystem::remove 删文件场景语义一致。注意：std::filesystem::remove 也能删空目录，但删目录应改用 RemoveDir"
  fix_suggestion: "改用 base 库等价实现：RemoveFile"
  references:
    - "common/include/file_util.h"
  tapd_ids: ["1070112211134320991"]
  tags: ["base-reuse", "std-to-base", "generated"]
  std_signature: "std::filesystem::remove"
  base_preferred: "util::file::RemoveFile"
  reason: "util::file::RemoveFile 删除指定文件，与 std::filesystem::remove 删文件场景语义一致。注意：std::filesystem::remove 也能删空目录，但删目录应改用 RemoveDir"
