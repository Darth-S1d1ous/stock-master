# 由 scripts/gen_rules_from_std_to_base.rs 从 std_to_base.jsonl 生成（勿手改）。
# 源记录 1 条；base 工厂：GetParentDir。
id: CPP_BASE_REUSE_STD_PATH_PARENT_PATH
enabled: true
name: "std::filesystem::path::parent_path 的 base 库等价实现要求"
severity: warn
engine_requires: [ast]
applies_to:
  languages: [cpp]
  exclude_glob:
    - "**/third_party/**"
    - "**/vendor/**"
    - "**/tests/**"
patterns:
  - id: std_call
    description: "调用 std::filesystem::path::parent_path（限定或裸调用；方法调用 .parent_path() 不在内；path::parent_path 限定名消歧同名 API）"
    spec:
      type: Call
      value:
        name: "path::parent_path"
meta:
  rationale: "util::file::GetParentDir 获取指定路径的父目录，与 std::filesystem::path::parent_path 语义一致"
  fix_suggestion: "改用 base 库等价实现：GetParentDir"
  references:
    - "common/include/file_util.h"
  tapd_ids: ["1070112211134320991"]
  tags: ["base-reuse", "std-to-base", "generated"]
  std_signature: "std::filesystem::path::parent_path"
  base_preferred: "util::file::GetParentDir"
  reason: "util::file::GetParentDir 获取指定路径的父目录，与 std::filesystem::path::parent_path 语义一致"
