# 由 scripts/gen_rules_from_std_to_base.rs 从 std_to_base.jsonl 生成（勿手改）。
# 源记录 1 条；base 工厂：GetTickTimestampMsec。
id: CPP_BASE_REUSE_STD_STEADY_CLOCK_NOW
enabled: true
name: "std::chrono::steady_clock::now 的 base 库等价实现要求"
severity: warn
engine_requires: [ast]
applies_to:
  languages: [cpp]
  exclude_glob:
    - "**/third_party/**"
    - "**/vendor/**"
    - "**/tests/**"
patterns:
  - id: std_call
    description: "调用 std::chrono::steady_clock::now（限定或裸调用；方法调用 .now() 不在内；steady_clock::now 限定名消歧同名 API）"
    spec:
      type: Call
      value:
        name: "steady_clock::now"
meta:
  rationale: "std::chrono::steady_clock::now 单调时钟；base util::time::GetTickTimestampMsec 提供等价单调时间戳"
  fix_suggestion: "改用 base 库等价实现：GetTickTimestampMsec"
  references:
    - "common/include/time_util.h"
  tapd_ids: ["1070112211134320991"]
  tags: ["base-reuse", "std-to-base", "generated"]
  std_signature: "std::chrono::steady_clock::now"
  base_preferred: "util::time::GetTickTimestampMsec"
  reason: "std::chrono::steady_clock::now 单调时钟；base util::time::GetTickTimestampMsec 提供等价单调时间戳"
