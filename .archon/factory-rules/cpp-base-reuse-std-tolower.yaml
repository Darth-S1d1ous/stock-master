# 由 scripts/gen_rules_from_std_to_base.rs 从 std_to_base.jsonl 生成（勿手改）。
# 源记录 1 条；base 工厂：ToLower。
id: CPP_BASE_REUSE_STD_TOLOWER
enabled: true
name: "std::tolower 的 base 库等价实现要求"
severity: warn
engine_requires: [ast]
applies_to:
  languages: [cpp]
  exclude_glob:
    - "**/third_party/**"
    - "**/vendor/**"
    - "**/tests/**"
patterns:
  - id: std_call
    description: "调用 std::tolower（限定或裸调用；方法调用 .tolower() 不在内；tolower 限定名消歧同名 API）"
    spec:
      type: Call
      value:
        name: "tolower"
meta:
  rationale: "std::tolower 字符转小写；base util::convert::ToLower 提供等价转换"
  fix_suggestion: "改用 base 库等价实现：ToLower"
  references:
    - "common/include/convert_util.h"
  tapd_ids: ["1070112211134320991"]
  tags: ["base-reuse", "std-to-base", "generated"]
  std_signature: "std::tolower"
  base_preferred: "util::convert::ToLower"
  reason: "std::tolower 字符转小写；base util::convert::ToLower 提供等价转换"
