# 由 scripts/gen_rules_from_std_to_base.rs 从 std_to_base.jsonl 生成（勿手改）。
# 源记录 1 条；base 工厂：MakeTime。
id: CPP_BASE_REUSE_STD_LOCALTIME
enabled: false
name: "std::localtime 的 base 库等价实现要求"
severity: warn
engine_requires: [ast]
applies_to:
  languages: [cpp]
  exclude_glob:
    - "**/third_party/**"
    - "**/vendor/**"
    - "**/tests/**"
patterns:
  - id: std_call
    description: "调用 std::localtime（限定或裸调用；方法调用 .localtime() 不在内；localtime 限定名消歧同名 API）"
    spec:
      type: Call
      value:
        name: "localtime"
meta:
  rationale: "std::localtime 将 time_t 转为本地时间 tm 结构，MakeTime 将秒数转本地时间 tm，语义精确对应"
  fix_suggestion: "改用 base 库等价实现：MakeTime"
  references:
    - "common/include/time_util.h"
  tapd_ids: ["1070112211134320991"]
  tags: ["base-reuse", "std-to-base", "generated"]
  std_signature: "std::localtime"
  base_preferred: "util::time::MakeTime"
  reason: "std::localtime 将 time_t 转为本地时间 tm 结构，MakeTime 将秒数转本地时间 tm，语义精确对应"
